apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nws-data.fullname" . }}-test-connection"
  labels:
    {{- include "nws-data.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nws-data.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
